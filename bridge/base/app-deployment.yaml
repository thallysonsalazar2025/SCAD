#! app-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: app
    namespace: scad
    labels:
        com.docker.compose.project: scad
        com.docker.compose.service: app
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: scad
            com.docker.compose.service: app
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: scad
                com.docker.compose.service: app
                com.docker.compose.network.scad-network: "true"
        spec:
            containers:
                - name: app
                  image: scad-app
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: DB_PASSWORD
                      value: "root"
                    - name: DB_USER
                      value: "root"
                    - name: GOOGLE_CLIENT_ID
                      value: "root"
                    - name: GOOGLE_CLIENT_SECRET
                      value: "124376"
                    - name: URL_DATABASE
                      value: "jdbc:mysql://localhost:3307/cadastro_produtos-prod"
                  ports:
                    - name: app-8080
                      containerPort: 8080
